package com.xirigo.ecommerce.core.designsystem.theme

import androidx.compose.ui.unit.dp

object XGCornerRadius {
    val None = 0.dp // cornerRadius.none
    val Small = 6.dp // cornerRadius.small
    val Medium = 10.dp // cornerRadius.medium — standard for all interactive elements
    val Large = 16.dp // cornerRadius.large
    val Pill = 28.dp // cornerRadius.pill — search bar, toggles
    val Toggle = 22.dp // cornerRadius.toggle
    val Full = 999.dp // cornerRadius.circle — full circle
}
