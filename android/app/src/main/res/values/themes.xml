<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme -->
    <style name="Theme.MoltMarketplace.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MoltMarketplace</item>
    </style>

    <!-- Main App Theme -->
    <style name="Theme.MoltMarketplace" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
