import Factory
import Foundation

// MARK: - Dependency Injection Container
// Factory container extensions for registering dependencies
// Actual registrations will be added as features are implemented

extension Container {
    // Base dependencies will be registered here in M0-03 (Network layer)
    // Feature-specific dependencies will be registered in their respective modules
}
